import React from 'react';
import { StyleSheet, ScrollView } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { useTranslation } from 'react-i18next';
import { ThemedView } from '@/components/atoms/ThemedView/ThemedView';
import { ThemedText } from '@/components/atoms/ThemedText/ThemedText';

export default function {{pascalCase name}}Screen() {
  const { t } = useTranslation();

  return (
    <SafeAreaView style={styles.safeArea}>
      <ScrollView style={styles.container}>
        <ThemedView style={styles.content}>
          <ThemedText type="title">{{pascalCase name}}</ThemedText>
          <ThemedText>Welcome to the {{pascalCase name}} tab!</ThemedText>
          
          <ThemedView style={styles.section}>
            <ThemedText type="subtitle">Section 1</ThemedText>
            <ThemedText>Add your content here...</ThemedText>
          </ThemedView>
        </ThemedView>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
  },
  container: {
    flex: 1,
  },
  content: {
    padding: 20,
    paddingBottom: 100, // Space for floating tab bar
  },
  section: {
    marginTop: 24,
    padding: 16,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: '#E5E5E5',
  },
});