import React from 'react';
import { StyleSheet, ScrollView } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { useTranslation } from 'react-i18next';
import { StyledView } from '@/components/atoms/StyledView';
import { StyledText } from '@/components/atoms/StyledText';

export default function {{pascalCase name}}Screen() {
  const { t } = useTranslation();

  return (
    <SafeAreaView style={styles.safeArea}>
      <ScrollView style={styles.container}>
        <StyledView style={styles.content}>
          <StyledText type="title">{{pascalCase name}}</StyledText>
          <StyledText>Welcome to the {{pascalCase name}} tab!</StyledText>
          
          <StyledView style={styles.section}>
            <StyledText type="subtitle">Section 1</StyledText>
            <StyledText>Add your content here...</StyledText>
          </StyledView>
        </StyledView>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
  },
  container: {
    flex: 1,
  },
  content: {
    padding: 20,
    paddingBottom: 100, // Space for floating tab bar
  },
  section: {
    marginTop: 24,
    padding: 16,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: '#E5E5E5',
  },
});