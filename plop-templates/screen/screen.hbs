import React from 'react';
import { StyleSheet } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { useTranslation } from 'react-i18next';
import { StyledView } from '@/components/atoms/StyledView';
import { StyledText } from '@/components/atoms/StyledText';

export default function {{pascalCase name}}Screen() {
  const { t } = useTranslation();

  return (
    <SafeAreaView style={styles.safeArea}>
      <StyledView style={styles.container}>
        <StyledText type="title">{{pascalCase name}} Screen</StyledText>
        <StyledText>Welcome to the {{pascalCase name}} screen!</StyledText>
      </StyledView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
  },
  container: {
    flex: 1,
    padding: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
});